---
name: "set_bgp_neighbors"
description: "Set a BGP neighbor on a device."
action_ref: "chatops_demo.set_bgp_neighbor"
formats:
  - "set bgp neigh {{hosts}} {{peer}} {{peer_asn}} {{commit_changes=' '}}"
  - "set bgp neighbors {{hosts}} {{peer}} {{peer_asn}} {{commit_changes=' '}}"
result:
  format: "{~}{{ execution.result.stdout.pretty }} "
