---
name: update_repo
pack: chatops_demo
runner_type: local-shell-cmd
description: Updates the repo that contains this demo
enabled: true
parameters:
  cmd:
    description: Command to run
    immutable: true
    default: git pull
  cwd:
    description: Working directory
    immutable: true
    default: /tmp/chatops_demo
